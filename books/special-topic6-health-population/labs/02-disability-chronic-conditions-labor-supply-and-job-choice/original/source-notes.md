# Source Notes

The lab uses no external source files and no confidential administrative or employer records. The synthetic panel is generated by `src/disability_chronic_conditions_lab.py` so the bounded teaching path is reproducible on any local checkout with Python.

The teaching design is anchored to the course readings, but it is not an official replication package.
