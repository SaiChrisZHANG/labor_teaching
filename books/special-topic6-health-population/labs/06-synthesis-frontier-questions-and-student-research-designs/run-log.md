# Week 6 Lab Run Log

The Week 6 lab is designed to run locally with the `research` conda environment:

```bash
ENV_NAME=research bash smoke.sh
```

The smoke path regenerates deterministic synthetic data and writes the reproduced, diagnosed, and transfer outputs.
