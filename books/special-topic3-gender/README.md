# Gender Study

This directory is the standalone MyST / Jupyter Book shell for the Gender Study special-topics course.

## Local build

From this directory:

```bash
conda run -n research jupyter book build --html --strict
```

## Note

`references.bib` is course-local and should be the destination for future weekly minimal reading packs.
