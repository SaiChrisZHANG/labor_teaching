# Week 5 Lab

Bounded synthetic teaching path for migration, local labor demand, and urban labor reallocation.

Run:

```bash
ENV_NAME=research bash smoke.sh
```

The script writes deterministic CSV outputs under `output/`.
