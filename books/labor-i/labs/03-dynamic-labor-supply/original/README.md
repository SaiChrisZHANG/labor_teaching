# Original path notes

This folder documents the Week 3 reproduction anchor.

- `reduced/` is populated by `src/build_week3_synthetic_data.py`.
- The bounded teaching file is synthetic and exists to keep the workflow runnable.
- The source paper benchmark is `@fehrGoette2007`.
