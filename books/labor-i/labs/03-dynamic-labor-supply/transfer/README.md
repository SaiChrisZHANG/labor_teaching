# Transfer path notes

The Week 3 transfer exercise keeps the same design family but changes the persistence environment.

- `data/` is populated by `src/build_week3_synthetic_data.py`.
- The transfer output compares temporary bonus responses across low- and high-adjustment-cost settings.
- The optional lifecycle extension in `lab.md` points students toward `@attanasioLowSanchezMarcos2008`.
