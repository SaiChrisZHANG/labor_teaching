# Reduced pedagogical data path

`composition_panel_synthetic.csv` is a synthetic cell-level panel that exists only to make the reproduction pipeline concrete before any real reduced teaching extract is deposited.

Expected columns:

- `date`
- `cell`
- `employment_share`
- `mean_log_wage`
- `base_weight`

A future instructor-provided reduced file can replace the synthetic one without changing the script interface.
