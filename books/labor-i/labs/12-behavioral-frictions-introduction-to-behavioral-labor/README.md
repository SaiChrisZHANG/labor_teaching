# Week 12 Lab Folder

This folder contains the bounded local teaching workflow for Week 12 of Labor I.

- `lab.md` is the student-facing handout.
- `src/` contains the deterministic synthetic-data builder and the reproduce/transfer scripts.
- `smoke.sh` runs the narrow local path used for validation.
