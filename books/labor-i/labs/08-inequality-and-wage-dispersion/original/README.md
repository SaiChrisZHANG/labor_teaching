# Original folder README

This folder stores the bounded Week 8 reproduction inputs and notes.

- `source-notes.md` explains the paper benchmark and what the synthetic file preserves.
- `reduced/autor_katz_kearney_inequality_synthetic.csv` is generated locally by `src/build_week8_synthetic_data.py`.

The synthetic file is built for teaching inequality measurement and decomposition logic. It is not a literal release of the original paper's underlying data.
