# Transfer Inputs

The pay-comparison, culture-sorting, and manager-transmission transfer inputs are generated locally by `src/build_week6_synthetic_data.py`.

They are synthetic teaching analogs, not official data from the cited papers.
