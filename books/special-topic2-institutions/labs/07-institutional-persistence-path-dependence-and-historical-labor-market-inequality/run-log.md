# Week 7 Lab Run Log

The canonical smoke command is:

```bash
ENV_NAME=research bash smoke.sh
```

The smoke test writes outputs under `output/reproduced/`, `output/diagnostics/`, and `output/transfer/`.
