# Transfer Data Notes

`transfer/data/historical_designs_synthetic.csv` is generated by `src/build_week7_synthetic_data.py`. Students may add rows for new historical labor settings after running the baseline smoke test.
