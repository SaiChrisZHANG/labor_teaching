# Week 2 Lab Run Log

Record local runs here.

```bash
ENV_NAME=research bash smoke.sh
```

Latest smoke result: passed. The script wrote reproduction outputs to `output/reproduced/` and transfer outputs to `output/transfer/`.
