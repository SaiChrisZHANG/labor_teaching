# Week 3 Lab Run Log

The Week 3 lab is designed to run locally with the `research` conda environment:

```bash
ENV_NAME=research bash smoke.sh
```

The smoke path regenerates deterministic synthetic data and writes reproduced, diagnosed, and transfer outputs.
