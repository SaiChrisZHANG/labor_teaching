# Week 10 lab run log

Use this file to record local runs of the smoke path and any deviations from the default teaching workflow.

## Standard smoke command

```bash
ENV_NAME=research bash smoke.sh
```

## Notes

- The data are deterministic synthetic teaching files generated by `src/make_synthetic_data.py`.
- Output files are written under `output/reproduced/` and `output/transfer/`.
- The lab reproduces DML and heterogeneity design logic; it is not an official replication package.
