# Original Teaching Path

The original path uses `original/reduced/online_labor_synthetic.csv`, a deterministic synthetic dataset for a DML exercise inspired by online labor-market research.

Run the full path with:

```bash
ENV_NAME=research bash smoke.sh
```

Key outputs are written to `output/reproduced/`.
