# Transfer Teaching Path

The transfer path uses `transfer/data/youth_program_synthetic.csv`, a deterministic synthetic dataset for studying treatment-effect heterogeneity in a youth employment program.

Run the full path with:

```bash
ENV_NAME=research bash smoke.sh
```

Key outputs are written to `output/transfer/`.
