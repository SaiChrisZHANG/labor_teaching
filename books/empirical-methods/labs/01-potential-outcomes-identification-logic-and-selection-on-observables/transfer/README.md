# Transfer Exercise

The transfer exercise applies the same selection-on-observables logic to workplace training take-up.

The synthetic transfer file is generated by `src/make_synthetic_data.py` and written to `transfer/data/workplace_training_synthetic.csv`. Students can replace it with a real bounded dataset if they document provenance and variable construction in `data-notes.md`.
