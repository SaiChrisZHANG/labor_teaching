# Transfer Teaching Inputs

This folder contains synthetic data for the Week 15 job-access transfer task.

The files are generated by:

```bash
python src/make_synthetic_data.py
```

The transfer exercise asks students to compare buffer access, distance-decay access, and travel-time access without using a live routing API.
