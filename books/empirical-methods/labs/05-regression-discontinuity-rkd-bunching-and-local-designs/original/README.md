# Original and reduced data

The public teaching path uses deterministic synthetic data generated by `src/make_synthetic_data.py`.

The reduced dataset is:

```text
original/reduced/close_election_synthetic.csv
```

It is inspired by close-election RD designs but is not an official replication file.
