# Transfer data

The transfer path uses deterministic synthetic tax-kink data generated by `src/make_synthetic_data.py`.

The transfer dataset is:

```text
transfer/data/tax_kink_bunching_synthetic.csv
```

It is inspired by bunching designs around nonlinear tax schedules but is not an official replication file.
