# Transfer Teaching Path

The transfer path uses deterministic synthetic data for a simplified schooling/work career-choice problem inspired by dynamic human-capital models. It is not an official replication of Keane and Wolpin or any other paper.

The data are regenerated by:

```bash
python src/make_synthetic_data.py
```

The main file is `transfer/data/career_choice_synthetic.csv`.
