# Original Teaching Path

The reproduce path uses deterministic synthetic data inspired by dynamic replacement models. It is not an official replication of Rust or any other paper.

The data are regenerated by:

```bash
python src/make_synthetic_data.py
```

The main file is `original/reduced/replacement_synthetic.csv`.
