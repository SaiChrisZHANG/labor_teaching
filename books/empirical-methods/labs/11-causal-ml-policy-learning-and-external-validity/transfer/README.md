# Transfer Teaching Path

This folder holds the shifted target-population data for the Week 11 transportability exercise.

Run:

```bash
python src/make_synthetic_data.py
```

from the lab root to regenerate `data/job_training_target_synthetic.csv`.
