# Original synthetic source data

The source data are generated by:

```bash
python src/make_synthetic_data.py
```

The generated file is:

```text
original/reduced/labor_mismatch_documents_synthetic.csv
```

Each record is a synthetic labor-market document bundle with a job or case-review note, benchmark mismatch label, benchmark mismatch type, and downstream placement-interest outcome.
