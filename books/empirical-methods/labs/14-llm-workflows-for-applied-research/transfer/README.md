# Transfer synthetic data

The transfer data are generated by:

```bash
python src/make_synthetic_data.py
```

The generated file is:

```text
transfer/data/workforce_case_notes_synthetic.csv
```

The transfer sample uses workforce-agency case-note language rather than source-domain job-match language. It is intentionally close enough to be tempting and different enough to require transport diagnostics.
