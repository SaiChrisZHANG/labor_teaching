# Transfer Teaching Data

The smoke test writes deterministic synthetic referral dyads to `transfer/data/referral_dyads_synthetic.csv`.

The file is a teaching analogue for referral-network designs. It is not an official replication package.
