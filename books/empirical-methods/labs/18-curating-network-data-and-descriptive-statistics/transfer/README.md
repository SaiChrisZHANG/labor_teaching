# Transfer teaching path

This folder contains synthetic directed referral data for the Week 18 transfer task.

The data are generated by:

```bash
python src/make_synthetic_data.py
```

The generated file is:

- `data/referrals_synthetic.csv`

The transfer task asks students to redefine the network object for a directed referral mechanism.
