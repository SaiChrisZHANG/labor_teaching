# Transfer data

The transfer path uses deterministic synthetic shift-share data generated by `src/make_synthetic_data.py`.

Generated files:

- `data/shift_share_places.csv`
- `data/sector_shocks.csv`

Students use these files to construct a shift-share instrument, diagnose the shock and exposure structure, and compare the interpretation to leave-out leniency designs.
