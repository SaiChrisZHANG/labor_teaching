# Transfer data

The shifted transfer file is generated by `src/make_synthetic_data.py`.

```text
transfer/data/service_records_shifted_synthetic.csv
```

It contains synthetic service-record text, institutional subgroup variables, an image-based infrastructure score, an external audit proxy, and a benchmark label for flexible service capacity.
